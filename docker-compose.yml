version: "3.7"

services:

  flask:
    build: ./flask
    container_name: flask
    restart: always
    environment:
      - APP_NAME=LED_API
    expose:
      - 9999

  nginx:
    build: ./nginx
    container_name: nginx
    restart: always
    ports:
      - "42069:42069"
      - "80:80"
